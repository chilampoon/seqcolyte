"""Protocol -> structured spec. Parses the checked-in scg_lib_structs HTML, cross-checks every
parsed sequence against independently-verified authoritative values, and emits a canonical JSON spec."""
